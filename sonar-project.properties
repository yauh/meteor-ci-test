# Required metadata
sonar.projectKey=de.yauh.meteor-ci-test
sonar.projectName=Meteor CI Test Project
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=app

# Language
sonar.language=js

# Encoding of sources files
sonar.sourceEncoding=UTF-8